import React from "react";
import Portfolio from "Portfolio";

const Project = (props) => {
  return (
    <div>
      <div>{props.id}</div>
      <div>{props.title}</div>
    </div>
    )
    project.map((id, title) => {<Project id={id} title={title}/>})
  }
  

export default Portfolio;
